# 백로그 & 일정 (작업 보드)

> **역할:** 머리에만 두지 않고, 여기만 보면 “뭘 나중에 할지 / 언제쯤 할지”가 잡히게 정리합니다.  
> **제품 스펙:** [PRD.md](./PRD.md) · **기술:** [IMPLEMENTATION.md](./IMPLEMENTATION.md)

---

## 1. 우선순위 태그 (짧게 쓰기)

| 태그 | 의미 |
|------|------|
| **P0** | 당장 막히거나 돈·보안·법과 직결 |
| **P1** | MVP 완성도·사용자 신뢰에 큰 영향 |
| **P2** | 있으면 좋음, 시간 나면 |
| **Later** | 확장 단계(체인·IPFS 등) — [README 로드맵](../README.md)과 동일 축 |

---

## 2. 이번 주 / 다음 스프린트 (채워 넣기)

> 날짜는 본인 일정에 맞게만 적으면 됩니다. 비우고 두어도 됨.

| 기간 | 목표 한 줄 | 비고 |
|------|------------|------|
| 2026-04-04 기준 직후 | 휴식 / 운영 모니터링 | Vercel `NAVER_*`·탈퇴 마이그레이션 적용 여부 확인 |
| 다음 | 네이버 로그인 검수 이후 | 메인(/) 히어로 등에 플랫폼명 재노출할지 PR·카피 검토 |

---

## 3. 백로그 (상태만 관리)

### 제품·비즈니스

| 항목 | 우선순위 | 상태 | 메모 |
|------|----------|------|------|
| **메인(/) 랜딩 — 로그인 상태 UX** | P1 | 미착수 | 대시보드 등 로그인 후 `/`로 이동 시 **세션이 끊긴 것처럼 보이는 현상** 개선(헤더에 로그인 유지·대시보드/계정 노출 등). 추후 작업 |
| **랜딩 카피 — 네이버 검수 후** | P2 | 미착수 | 검수 완료 시 히어로/공지에 플랫폼 브랜드(네이버 시리즈 등) **재노출 여부**만 결정하면 됨(2026-04-04 전에는 브랜드 나열 제거해 둠) |
| NAT 유료 충전·패키지·웹훅 (`/billing` 실결제) | P1 | 미착수 | PG·환불 정책 정리 후 |
| 장르별 에이전트·프롬프트 고도화 | P2 | 미착수 | |
| 흥행작 학습 데이터·벤치마크 | P2 | 미착수 | |
| 작가 커뮤니티·댓글 등 | P2 | 미착수 | |
| 고득점 원고 → 매니지먼트 매칭 | Later | 아이디어 | PRD·README 확장 |

### 운영·안전

| 항목 | 우선순위 | 상태 | 메모 |
|------|----------|------|------|
| `POST /api/analyze` 레이트 리밋 | P1 | 미착수 | 비용·남용 방지 |
| `users.nat_balance` 클라이언트 직접 수정 차단 검토 | P1 | 미착수 | RLS·서버 전용 충전 |
| 어드민( NAT 수동 조정·사용자 검색 ) | P2 | 미착수 | 별도 역할·라우트 |

### 기술 부채·개선

| 항목 | 우선순위 | 상태 | 메모 |
|------|----------|------|------|
| Next.js middleware → proxy 마이그레이션 (경고 대응) | P2 | 미착수 | 빌드 시 deprecation 안내 |
| Agent Score vs 독자 행동 지표 분리 문서화 | P2 | 진행됨 일부 | explore는 분석 평균 기준 |

---

## 4. 마일스톤 (분기/월 단위 — 선택)

> 큰 덩어리만. 세부 일정은 위 표나 외부 캘린더와 병행.

| 시점(예정) | 마일스톤 | 완료 기준(예시) |
|------------|----------|-----------------|
| | MVP “핵심 루프” 고정 | 가입→작품→분석→리포트→NAT 차감 안정 |
| | 유료 NAT 베타 | 결제 1건 + 환불 플로우 |
| | | |

---

## 5. 끝난 일 (최근만 적기)

> 스트레스 줄이려고 “한 줄 요약”만.

- **2026-04-04 전후 묶음:** 회원 탈퇴(`POST /api/account/withdraw`·모달·`탈퇴합니다` 확인·NAT 삭제 안내 강조)·`users.deleted_at` 소프트 삭제·관련 SQL 마이그레이션. OAuth **로컬 개발** 시 `oauthCallbackOrigin`으로 localhost면 `NEXT_PUBLIC_SITE_URL` 대신 **현재 origin**을 `redirectTo`에 사용. **네이버 로그인** 기본 노출 + Vercel에 `NAVER_CLIENT_ID`/`SECRET` 없으면 `/api/auth/naver/start` JSON 오류(검수와 무관, 환경 변수 이슈). **랜딩(/):** 네이버 검수 전 히어로에서 플랫폼 브랜드 나열 제거 → 카피를 **「장르·연재 맥락에 맞춘 AI 흥행 분석」** 으로 단순화.
- (예) 원고 글자수 분기·Claude 통일·`users` 동기화·Agent Score=분석 평균 …
- **이 화 분석 연속성:** 앞선 회차 **분석 요약만** 컨텍스트로 주입(원고 전체 재전송 없음). `previous_episodes_context`·프롬프트·`/api/analyze` 연동.
- **일괄 통합 대용량:** 10화 초과 시 `analyze-batch-holistic-chunk`(최대 10화)·순차 처리·재시도 → `analyze-batch-holistic-merge`로 종합 리포트·`holistic_analysis_runs` 저장. UI: 진행률·ETA·병합 단계 표시. NAT: `estimateHolisticBatchTotalNat`로 배치+병합 안내.
- **문서:** 위 내용 README·본 백로그에 반영. **Cursor:** 모델 전환 ≠ 대화 삭제(README 개발 섹션 참고).

---

## 6. 외부 도구와 역할 나누기

| 도구 | 이 저장소 `BACKLOG.md`와 |
|------|-------------------------|
| **이 파일** | 무엇을 할지·대략 순서·우선순위 |
| **캘린더 / Notion / GH Issues** | 확정 일정·티켓 번호·담당 |
| **PRD** | “왜 하는지”는 PRD, “언제 할지”는 여기 또는 외부 |

*이 문서는 자주 고쳐도 됩니다. 완벽한 로드맵보다 **지금 기준 한 장**이 목표입니다.*
