/** Chroma 컬렉션 이름 — 인제스트·검색이 동일해야 함 */
export const TRENDS_COLLECTION_NAME = "webnovel-trends";

/** 원문 텍스트 상대 경로(앱 루트 기준) */
export const DEFAULT_TRENDS_DIR = "data/trends";

/** 로컬 Chroma 서버 영속 디렉터리(문서용 — `chroma run --path` 와 맞춤) */
export const DEFAULT_CHROMA_PERSIST_PATH = "data/chroma_db";
